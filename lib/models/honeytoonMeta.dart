class HoneytoonMeta {
  String workId;
  String uid; // userid
  String displayName;
  String coverImgUrl;
  int totalCount;
  String title;
  String description;
  
  
  HoneytoonMeta({this.workId, this.uid, this.displayName, this.coverImgUrl, this.totalCount, this.title, this.description}){

  }
}